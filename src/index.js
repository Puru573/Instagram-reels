import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Use the old rendering method
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
